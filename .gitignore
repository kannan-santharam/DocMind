# dependencies & build output
node_modules
.next
out
dist
*.tsbuildinfo
next-env.d.ts
.vercel

# secrets — never commit
.env
.env.local
.env*.local

# preloaded documents are personal content, not source.
# The folder and its README stay so a fresh clone knows what goes here.
docs/seed/*
!docs/seed/README.md

# generated by `next dev`, not authored
AGENTS.md
CLAUDE.md

.DS_Store
